# Copy to .env.local and fill in values
# (Expo reads EXPO_PUBLIC_* vars at build time)

# API base URL (no trailing slash, no /api suffix — that's added by client.ts)
EXPO_PUBLIC_API_URL=http://localhost:8000/api

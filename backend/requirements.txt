# Django Backend Core
Django==4.2.7
djangorestframework==3.14.0
gunicorn==22.0.0

# Database Drivers
psycopg2-binary

# API and Security
requests==2.31.0
Pillow
django-cors-headers==4.3.1
django-filter==23.5

# Authentication
dj-rest-auth[with_social]
djangorestframework-simplejwt
django-allauth

# Environment Management
python-decouple==3.8
python-dotenv

# Pokemon TCG SDKs
pokemontcgsdk
tcgdex-sdk

# AI Pre-grading (Phase 3)
opencv-python-headless==4.10.0.84  # image preprocessing — no GUI needed
numpy>=1.26,<3                     # array ops; already a transitive dep

# Phase 3: S3 photo storage
boto3>=1.34

# Payments
stripe>=9.0

# Optional: install for model training and production inference
# torch==2.3.0                     # PyTorch CPU build (~200 MB wheel)
# torchvision==0.18.0              # EfficientNet pretrained weights
# scikit-learn>=1.4                # metrics (f1_score, classification_report)
